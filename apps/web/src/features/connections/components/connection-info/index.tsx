import { ConnectionInfoFooter } from './connection-info.footer'
import { ConnectionInfoHealth } from './connection-info.health'
import { ConnectionInfoProvider } from './connection-info.provider'
import { ConnectionInfoType } from './connection-info.type'

export const ConnectionInfo = {
  Provider: ConnectionInfoProvider,
  Type: ConnectionInfoType,
  Health: ConnectionInfoHealth,
  Footer: ConnectionInfoFooter,
}
